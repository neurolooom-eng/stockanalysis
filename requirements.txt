fastapi>=0.110
uvicorn>=0.27
pandas>=2.0
requests>=2.31

# Deliberately NOT pinned. Yahoo Finance changes its endpoints often and
# yfinance ships fixes quickly. If prices stop loading, first try:
#     pip install -U yfinance
yfinance

# Optional - only needed if you use the AI analysis, and only the one whose
# provider you pick. The app imports them lazily, so it runs fine without both.
anthropic
openai
